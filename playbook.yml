- hosts: localhost
  connection: local
  roles:
  - macbook
  vars:
    gitconfig:
      - filename: .gitconfig-extra-filename
        username: 
        email: 
    sshconfig:
      - host: 
        hostname: 
        identity: 