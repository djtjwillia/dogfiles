[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[user]
	name  = "Taylor J Williams"
	email = 
[pager]
  branch = false
# [includeIf "gitdir:<directory to trigger load of alternet gitconfig>"]
# 	path = <path to alternate gitconfig>
# [includeIf "gitdir:<directory to trigger load of alternet gitconfig>"]
# 	path = <path to alternate gitconfig>
[init]
    defaultBranch = main
[alias]
    lga = log --abbrev-commit --decorate --graph --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset)%C(white)%s %C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    lg 	= log --abbrev-commit --decorate --graph --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset)%C(white)%s %C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)'
    rename = branch -m
[push]
	autoSetupRemote = true
